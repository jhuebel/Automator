@page "/status"
@attribute [Authorize]
@rendermode InteractiveServer
@inject NavigationManager Nav

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/settings?tab=status", replace: true);
}
